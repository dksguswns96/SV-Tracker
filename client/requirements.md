## Packages
framer-motion | Smooth entry animations and layout transitions
recharts | Data visualization for sales trends
date-fns | Date formatting helper

## Notes
Scraper trigger expects POST /api/scraper/trigger
Sales data fetched via GET /api/sales with optional query params
Images are external URLs or placeholders
